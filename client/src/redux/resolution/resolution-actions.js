import { createAction } from "@reduxjs/toolkit";

export const mobileResolution = createAction("resolution/mobileResolution");

export const tabletResolution = createAction("resolution/tabletResolution");
